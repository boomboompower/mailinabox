# Fail2Ban filter for Mail-in-a-Box encryption auth failures.
# Matches failed recovery code and password attempts on the encryption endpoints.

[INCLUDES]

before = common.conf

[Definition]

_daemon = mailinabox

failregex = Mail-in-a-Box Management Daemon: Encryption auth failure from ip <HOST> - timestamp .*
ignoreregex =
