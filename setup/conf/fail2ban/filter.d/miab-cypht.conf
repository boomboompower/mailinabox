[Definition]
# Matches failed Cypht webmail login attempts written by the
# Hm_Handler_log_failed_login patch in setup/webmail/cypht.sh.
# Format: [YYYY-MM-DD HH:MM:SS] Failed login for user@example.com from 1.2.3.4
failregex = ^\[[^\]]*\] Failed login for [^\n]+ from <HOST>$
ignoreregex =
